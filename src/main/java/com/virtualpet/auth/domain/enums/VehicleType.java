package com.virtualpet.auth.domain.enums;

/** Vehicle a rider uses for deliveries; mirrors the chk_vehicle_type constraint on auth.riders. */
public enum VehicleType {
  MOTO,
  BICI,
  CAMIONETA,
  AUTO
}
